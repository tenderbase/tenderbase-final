Replace the existing /api/v1/buyers implementation with a relationship-driven query. The current endpoint incorrectly enumerates Organization rows, while Tender records can identify the actual buyer relationship. The directory must JOIN Organization to Tender on Tender.buyerId = Organization.id, group by organization fields, support q, page, and limit, and return tenderCount. The buyer detail/tenders route should resolve the organization by id and query Tender.buyerId directly.