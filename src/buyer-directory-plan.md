Buyer directory fix plan: use Tender.buyerId as the authoritative relationship. Enumerate organizations through JOIN Tender rather than Organization alone. Group by Organization.id and expose tenderCount. q searches Organization.name. Detail verifies Organization.id has buyer-linked tenders. Buyer tenders queries Tender.buyerId directly. Preserve pagination and existing response shape where possible.